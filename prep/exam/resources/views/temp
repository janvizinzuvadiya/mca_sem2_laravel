 <div class="row">
            <div class="col-lg-8">
              <h1 class="h1 text-primary">Employee List</h1>  
            </div>
            <div class="col-lg-4">
              <a href="{{ route('emp.reg') }}" class="btn btn-primary">Add Employee</a>
            </div>
          </div>
        
        <table class="table table-striped mt-5">
          <thead>
            <tr>
              <th>ID</th>
              <th>Name</th>
              <th>Email</th>
              <th>Phone</th>
              <th>Designation</th>
              <th>Salary</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            @foreach ($employee as $emp)
            <tr>
              <td>{{$emp->id}}</td>
              <td>{{$emp->name}}</td>
              <td>{{$emp->email}}</td>
              <td>{{$emp->phone}}</td>
              <td>{{$emp->designation}}</td>
              <td>{{$emp->salary}}</td>
              <td>
                  <a href="{{ route('emp.edit',$emp->id) }}" class="btn btn-primary">Edit</a>
                  <a href="{{ route('emp.delete',$emp->id) }}" class="btn btn-danger">Delete</a>
              </td>
            </tr>
          @endforeach
          </tbody>
        </table>
      </div>